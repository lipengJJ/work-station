fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
apscheduler>=3.10
python-jose[cryptography]>=3.3
bcrypt>=4.1
python-multipart>=0.0.9
pydantic-settings>=2.2

# xhs 采集（Spider_XHS 核心依赖，创作者中心相关的 opencv-python/numpy/aiohttp 用不到，跳过）
requests
loguru
python-dotenv
retry
openpyxl
PyExecJS
qrcode[pil]
Pillow
yfinance
lxml  # yfinance 的 earnings_dates（历史 EPS surprise）需要它解析
PyYAML  # 解析 Skill 的 SKILL.md frontmatter 和 workbench.yaml

# xhs 评论获取切换为 Playwright 页面级爬取（services/client/page_crawler.py）。
# 安装后还需执行 `python -m playwright install chromium`（Docker 构建见 Dockerfile）
playwright

# ai_trending 热点聚合：RSS 2.0 / Atom 统一解析（InfoQ / 36氪 / arXiv）
feedparser>=6.0
boto3>=1.34
numpy>=1.26  # 语义检索向量编解码与批量点积（显式声明，不依赖 yfinance 的传递依赖）
