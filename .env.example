# 复制为 .env（docker compose 会自动读取同目录下的 .env），并把下面的值换成你自己的。
# 生成一个随机密钥，例如: openssl rand -hex 32
WORKBENCH_SECRET_KEY=change-me-to-a-random-secret

# 一般不用改，只有和宿主机上别的服务端口冲突时才需要
BACKEND_PORT=8010
FRONTEND_PORT=80
